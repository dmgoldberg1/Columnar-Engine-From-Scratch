project_name       = "columnar-benchmark"
zone               = "ru-central1-d"
subnet_cidr_blocks = ["10.10.0.0/24"]

ssh_user            = "ubuntu"
ssh_public_key_path = "~/.ssh/id_ed25519.pub"

# Replace this documentation address with your current public IPv4 address.
# /32 means that only this single address is allowed to connect over SSH.
ssh_allowed_cidr_blocks = ["203.0.113.10/32"]
